CORPGRAPH_DB_URL=postgresql://corpgraph:corpgraph@localhost:5432/corpgraph

# API keys for LLM enrichment (optional — only needed if using --enrich)
# OPENAI_API_KEY=sk-...
# AWS credentials for Bedrock are read from standard AWS config (~/.aws/)
